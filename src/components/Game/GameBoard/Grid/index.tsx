import React from "react";
import { TGridInfo } from "../../../../types";

//style
import "./Grid.scss";
const Grid = ({ position, ship, fired }: TGridInfo): JSX.Element => {
  return <div className="Grid"></div>;
};

export default Grid;
